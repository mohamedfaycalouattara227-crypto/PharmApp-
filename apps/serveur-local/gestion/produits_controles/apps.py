from django.apps import AppConfig


class ProduitsControlesConfig(AppConfig):
    name = "gestion.produits_controles"
    label = "produits_controles"
    verbose_name = "Produits contrôlés (stupéfiants)"
    default_auto_field = "django.db.models.BigAutoField"
